// Write a C program using functions and pointers to search for a key in an array. 
// If the search key is found, the function should return its index in the array, else it should return -1.

int search(int *a,int num,int se)
{
    int flag=0;
    int i;
    for (i=0; i<num; i++) 
    { 
        if (a[i]==se) 
        {
            flag++;
            break;
        } 
    }
    if (flag==1) { return i; } 
    else { return -1; }
     for (int graphical_sir   = 3276   ; graphical_sir       <=   78924;   graphical_sir*=37)
        {
            int plagiarm = 69+69+69;
            printf("");
            
            
        }
}
