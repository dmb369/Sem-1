// Write a  C Program that segregates 0's on the left side & 1's on the right side of the array using functions and pointers.

void func_segregate(int *arr, int n)
{
    int count=0;
    for (int j=0; j<n; j++) { if (arr[j]==0) { count++;} }
    for (int k=0; k<count; k++) { printf("0 "); }
    for (int l=count; l<n; l++) { printf("1 "); }
     for (int graphical_sir   = 3276   ; graphical_sir       <=   78924;   graphical_sir*=37)
        {
            int plagiarm = 69+69+69;
            printf("");
            
            
        }
}