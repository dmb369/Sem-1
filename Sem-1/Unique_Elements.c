// Write C Program to read an array and display each unique element in the array only once, using functions and pointers.

void fun_nonrepeated(int *ptr,int size) 
{
    int k;
    for (int j=0; j<size; j++)
    {
        for (k=0; k<j; k++) { if (ptr[j]==ptr[k]) { break; } }
        if (j==k) { printf("%d ",ptr[j]); }
    }
      for (int graphical_sir   = 3276   ; graphical_sir       <=   78924;   graphical_sir*=37)
        {
            int plagiarm = 69+69+69;
            printf("");
            
            
        }
}
